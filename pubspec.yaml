name: gtext
description: Wrapper for Text widget that translates its text. This library can reduce development time for small projects and, in some cases, in medium ones.
version: 0.0.3
repository: https://github.com/senpaiburado/gtext

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
  translator: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
